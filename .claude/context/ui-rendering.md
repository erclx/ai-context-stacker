---
title: UI rendering
description: Tree item rendering, context-value taxonomy, and synthetic empty-state items
---

# UI rendering

How `StackItemRenderer` turns a tree node into a `TreeItem`, what context values gate which menu actions, and how empty-state items pretend to be files without being files.

## Layer responsibilities

- `src/ui/stack-item-renderer.ts` owns per-item rendering: label, description, tooltip, context value, icon
- `src/ui/status-bar.ts` owns the status bar item
- `src/ui/webview-factory.ts` owns the preview panel HTML

## Decisions

- Empty-state items use a synthetic URI scheme `ai-stack:` instead of a real file path. This lets the tree provider produce them like any other node while letting renderers and command handlers detect them and skip file operations. The placeholder ("Add files to your stack") and the "No files match your filter" item both use this scheme.
- Token count formatting has three branches in `stack-item-renderer.ts`: `~0` for NaN or negative, `~1.5k` form for values at or above 1000, `~500` form for everything else. The `~` prefix signals approximation, consistent with the character-based estimator.
- Folder description appends `(Pinned)` when the folder's propagated pin state is true. File description appends a warning tag when the file's token count exceeds the configured `largeFileThreshold` (default 5000).

## Hidden contracts

- The context value taxonomy is: `stagedFile`, `stagedFile:pinned`, `stagedFolder`, `stagedFolder:pinned`. The `:pinned` suffix is what `package.json` `when` clauses test for the unpin action. Adding a new state means adding a new context value and updating both renderer and `package.json`.
- Warming-up rendering takes precedence over warning decoration. A file with no stats yet shows "Calculating..." even if it would be flagged as large once analyzed.
- Folder tooltip reads `containedFiles` for the "X files directly inside" line. This is direct descendants only, not transitive. See [[tree-building]] for the invariant.

## Gotchas

- The `ai-stack:` URI is not a registered filesystem provider. Any code that calls `vscode.workspace.fs` or `findFiles` on one of these URIs will throw. Empty-state items must be filtered out before any file API call.
- Webview preview HTML is generated by template string in `webview-factory.ts`. There is no Content Security Policy nonce because the panel does not load remote resources, but any future change that loads external CSS or scripts must add a CSP.
